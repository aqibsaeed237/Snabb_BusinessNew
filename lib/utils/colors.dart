import 'package:flutter/material.dart';

/// A utility class for managing the color scheme of the application.

/// Dark gray color used for border radius, hint color, and icon color.
const Color darkGrayColor = Color.fromRGBO(136, 130, 130, 1);

/// Purchase color.
const Color darkBlueColor = Color.fromRGBO(68, 130, 255, 1);

/// Text color.
const Color blackColor = Color.fromRGBO(0, 0, 0, 1);

/// Light gray color used for text field borders.
const Color textFieldBorderLightGrayColor = Color.fromRGBO(196, 196, 196, 1);

/// Sale color.
const Color saleGreenColor = Color.fromRGBO(52, 168, 83, 1);

/// Expense color, delete button, and expense icon widget color.
const Color expenseRedColor = Color.fromRGBO(236, 85, 85, 1);

/// Cash flow color.
const Color cashFlowYellowColor = Color.fromRGBO(254, 164, 44, 1);

/// App bar, heading large, and button color border color width 1.
const Color primaryBlueColor = Color.fromRGBO(40, 109, 249, 1);

/// Container background color.
const Color extraLightGrayColor = Color.fromRGBO(242, 242, 242, 1);

/// Container background color for input text field.
const Color inputLabelTextFieldContainerColor =
    Color.fromRGBO(136, 130, 130, 1);

/// Hint color, icons of text field, border radius.
const Color hintLightGrayColor = Color.fromRGBO(118, 118, 118, 1);

/// Text color.
const Color whiteColor = Colors.white;
